# PR#<n> - <PR title>

AB#<work-item-id> · <work item title>

> <one-line summary of what this PR does and which screens/areas it touches>

🟢 PR #<n> — <open|draft|merged> · <author display name>

| Field         | Value |
|---------------|-------|
| Link          | [#<n> <commit-style title>](<github-pr-url>) |
| Author        | <Author Name> (<github-handle>) |
| Branch        | <head-branch> |
| Base          | <base-branch> |
| Review        | <Pending (draft) / Ready / Approved / Changes requested> |
| Checks        | <✅ passing / ❌ <check name> failing — summary of the rest> |
| Compatibility | <✅ Passed against <base>:<branch> / ❌ ...> |

> [!warning] <only include this block if something is blocking>
> <e.g. Detekt check is failing. PR is still a draft — needs detekt fixed
> before it can be marked ready for review.>

## Notes
- <reviewer comments, follow-ups, decisions — optional>

Tags: #pr #ab-<work-item-id>
```
Status: inprogress   (move to done/ and prefix [DONE] when merged)
Generated: <YYYY-MM-DD HH:MM> — snapshot, refresh with the generate prompt
```
