// Copy this file to Config.xcconfig and set TEAM_ID to your Apple Developer Team ID.
// Config.xcconfig is ignored by git, because the Team ID is personal to each machine.
// Find the Team ID in Xcode, under Settings > Accounts, or at developer.apple.com > Membership.
// The iOS Simulator does not need a Team ID. Leave TEAM_ID empty to build for the simulator only.

TEAM_ID=

PRODUCT_NAME=KompassKmp
PRODUCT_BUNDLE_IDENTIFIER=com.tekmoon.kompasskmp.KompassKmp$(TEAM_ID)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0
